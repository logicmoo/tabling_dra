
Shared Tests

All tests use full emulator paths (to distinguish 32 and 64 bits)

------------------------------------------------------------------------
Elem and Elem-Dyn

bash benchelem.sh <emulator> <maxthreads>
bash benchelemdyn.sh <emulator> <maxthreads>

maxthreads = 16, requires approximately 671 mbytes of trie code and 128 mbytes of tables. in 32 bits

------------------------------------------------------------------------
WF

bash benchwf.sh <emulator> <maxthreads>

maxthreads = 16, requires ~244K of trie code and 440 mbytes of tables. in 32 bits

------------------------------------------------------------------------
Omega

maxthreads = 16, requires ~893 mbytes of trie code and 322 mbytes of tables. in 32 bits

bash benchomega.sh <emulator> <maxthreads>

------------------------------------------------------------------------
Constraints

bash benchconstr.sh <emulator> <maxthreads>

maxthreads = 16, requires ~13 mbytes of trie code and 42 mbytes of tables. in 32 bits

------------------------------------------------------------------------
Preferences

bash benchprefs.sh <emulator> <maxthreads>

maxthreads = 16, requires ~1 mbytes of trie code and ~9 mbytes of tables. in 32 bits
