%% This file can be used in two ways:
%%
%%   1. Load the metainterpreter (colp), then invoke "prog( example )".
%%
%%   2. Load the translator (translate_colp), then invoke "tc( example )" and
%%      "tc( 'examples/comember' ).  This will produce file "example.ecl" that
%%      can be loaded and executed directly in Eclipse.

:- [ 'examples/comember' ].
